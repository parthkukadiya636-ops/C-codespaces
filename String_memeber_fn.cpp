// str.lenght()-> use to calculate the length of any functon.

// str.at(inx) -> gives the value or charcater at that string.

// str.substr(startidx, Size)-> gives the substring from starting index to the size given.

// str.find(word) -> find the word if present in the string else it will return -1.

// dynamic memory allocation :
 
int size;
cin>>size;

int *ptr = new int[size];

